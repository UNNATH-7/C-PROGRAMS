//program for printing factors count of a given number
#include<stdio.h>
main()
{
	int i,n,count=0;
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(n%i==0)
		{
			count++;
			printf("%d",i);
		}
	}
	printf("(%d)",count);
}
